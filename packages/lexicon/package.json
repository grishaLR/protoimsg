{
  "name": "@chatmosphere/lexicon",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "codegen": "lex gen-api --yes ./generated ./schemas/app/chatmosphere/chat/*.json",
    "codegen:check": "pnpm codegen && prettier --write 'generated/**/*.ts' && node -e \"const { execSync } = require('child_process'); const path = require('path'); const root = path.resolve(process.cwd(), '../..'); try { execSync('git diff --exit-code -- packages/lexicon/generated', { cwd: root }); } catch (e) { console.error('Generated lexicon files changed. Commit them after schema changes.'); process.exit(1); }\""
  },
  "devDependencies": {
    "@atproto/lex-cli": "^0.5.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@atproto/lexicon": "^0.6.1"
  }
}
