# chatmosphere Web Client
# No env vars needed for development — Vite proxy handles API/WS routing.
# For production, set VITE_API_URL if the API is on a different domain.
# VITE_API_URL=https://api.chatmosphere.app
